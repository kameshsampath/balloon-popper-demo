# https://taskfile.dev
# Copyright 2024-Present Kamesh Sampath
# Licensed under the Apache License, Version 2.0
#
# Namespace: task bronze:<task>
# Wire these to aws CLI, scripts, and tools/bronze-preload as the lab matures.

version: "3"

tasks:
  glue-setup:
    desc: AWS Glue prerequisites (database, job params); replace echo with aws/cli scripts
    cmds:
      - echo "TODO implement Glue setup (e.g. aws glue create-database …). See lab/bronze-landing-zone.md"

  s3tables-setup:
    desc: S3 bucket and S3 Tables / catalog registration; replace echo with aws or CFN
    cmds:
      - echo "TODO implement S3 Tables setup. See lab/bronze-landing-zone.md"

  load:
    desc: Load bronze Iceberg data (PyIceberg or Glue job)
    cmds:
      - echo "TODO wire to tools/bronze-preload (uv run …). See tools/bronze-preload/README.md"

  all:
    desc: Run glue-setup, s3tables-setup, then load in order
    deps:
      - glue-setup
      - s3tables-setup
      - load
